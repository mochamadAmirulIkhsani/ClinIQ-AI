version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g bun@1.3.14
            - cd $CODEBUILD_SRC_DIR && bun install
        build:
          commands:
            - cd $CODEBUILD_SRC_DIR && bunx turbo run build --filter=web
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - ../../node_modules/**/*
          - node_modules/**/*
          - .next/cache/**/*
